These DLLs are copied from those used by the original Labview VI. The only one used is AstriumComms.

It should be possible to recreate the DLL from the code in https://github.com/ISISComputingGroup/AstriumChopper.

However, this hasn't been done as the Astrium has a finite lifetime anyway.